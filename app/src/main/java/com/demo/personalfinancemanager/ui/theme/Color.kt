package com.demo.personalfinancemanager.ui.theme

import androidx.compose.ui.graphics.Color

// Primary Colors - Based on the app screenshot
val Primary = Color(0xFF1A1A1A)           // Dark primary for text and UI elements
val PrimaryLight = Color(0xFF2C2C2C)      // Slightly lighter for elevation
val Secondary = Color(0xFF6C63FF)          // Accent color for interactive elements

// Background Colors
val BackgroundLight = Color(0xFFF5F5F5)    // Light gray background
val BackgroundDark = Color(0xFF121212)     // Dark background for dark mode
val Surface = Color(0xFFFFFFFF)            // White surface for cards
val SurfaceDark = Color(0xFF1E1E1E)        // Dark surface for cards in dark mode

// Chart Colors
val ChartLine = Color(0xFFFFFFFF)          // White line for the chart
val ChartBackground = Color(0xFF1A1A1A)    // Dark background for chart
val ChartNegative = Color(0xFFFF6B6B)      // Red for negative/loss indicator

// Transaction Colors
val CreditGreen = Color(0xFF4CAF50)        // Green for income/credit
val DebitRed = Color(0xFFFF5252)           // Red for expense/debit

// Text Colors
val TextPrimary = Color(0xFF1A1A1A)        // Dark text for light mode
val TextSecondary = Color(0xFF6B6B6B)      // Gray text for subtitles
val TextOnDark = Color(0xFFFFFFFF)         // White text on dark backgrounds
val TextOnDarkSecondary = Color(0xFFB0B0B0) // Gray text on dark backgrounds

// Icon Colors
val IconPrimary = Color(0xFF1A1A1A)
val IconSecondary = Color(0xFF9E9E9E)
val IconOnDark = Color(0xFFFFFFFF)

// Bottom Navigation
val BottomNavBackground = Color(0xFF1A1A1A)  // Dark bottom navigation
val BottomNavSelected = Color(0xFFFFFFFF)    // White for selected item
val BottomNavUnselected = Color(0xFF6B6B6B)  // Gray for unselected items

// Divider
val DividerColor = Color(0xFFE0E0E0)

// FAB (Floating Action Button)
val FABBackground = Color(0xFF1A1A1A)
val FABIcon = Color(0xFFFFFFFF)
